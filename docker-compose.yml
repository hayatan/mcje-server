version: '3.6'

services:
  minecraft:
    build: .
    tty: true
    volumes:
      - ./data:/data
    environment:
      - EULA=TRUE
      - TYPE=FORGE
      - VERSION=1.16.4
      - WHITE_LIST=TRUE
    restart: always
    ports:
        - "25565:25565"